body {
    font-family: sans-serif;
    margin: 20px;
}

.add-form {
    margin-bottom: 20px;
}

#checklist li {
    list-style: none; /* 点を消す */
    margin-bottom: 10px;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

/* カテゴリーバッジのスタイル */
.category-badge {
    background-color: #eee;
    padding: 3px 8px;
    border-radius: 10px;
    font-size: 0.8em;
    margin-left: 10px;
}

/* (発展) カテゴリーごとに色を変える例 */
#checklist li[data-category="valuables"] {
    background-color: #fffbe6; /* 貴重品は薄い黄色 */
}
#checklist li[data-category="gadget"] {
    background-color: #e6f7ff; /* ガジェットは薄い青 */
}
